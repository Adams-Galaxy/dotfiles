# Non-package-manager manifests for shell bootstrap.
# For OS/package-manager packages, see packages.conf and `dotpkg`.
#
# Zsh plugins are declared in dot_config/zsh/zsh_plugins.txt and installed
# by antidote on first shell start, so there is nothing to list here.
