#!/usr/bin/env sh
set -eu

mkdir -p "$HOME/.zfunc"
mkdir -p "$HOME/.config/zsh/local"
mkdir -p "$HOME/.cache/zsh"

if [ -d "{{ .chezmoi.sourceDir }}/scripts" ]; then
	sh "{{ .chezmoi.sourceDir }}/scripts/bootstrap.sh" --install-only
fi
