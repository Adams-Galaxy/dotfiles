# Gruvbox Dark Hard Flavor for Yazi

This flavor is based on `gruvbox-material.yazi`, remapped to the workspace's dark-hard Gruvbox color set.

## Usage

In `~/.config/yazi/theme.toml`:

```toml
[flavor]
dark = "gruvbox-dark-hard"
```