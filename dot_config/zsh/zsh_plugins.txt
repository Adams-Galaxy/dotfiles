# Plugins that must load AFTER compinit.
#
# fzf-tab replaces zsh's completion menu with fzf. Per its README it has to
# come after compinit, but before anything that wraps ZLE widgets — so it
# must stay above the two below.
Aloxaf/fzf-tab

# These wrap ZLE widgets, so they load last. zsh-syntax-highlighting in
# particular only wraps widgets that exist when it loads, so it stays last.
zsh-users/zsh-autosuggestions
zsh-users/zsh-syntax-highlighting
