-- Seamless <C-h/j/k/l> movement between Neovim splits and tmux panes;
-- pairs with the tmux-side plugin declared in dot_tmux.conf.tmpl.
return {
  "christoomey/vim-tmux-navigator",
  lazy = false,
}
